<!DOCTYPE html>
<html>

<head>
    <title>Admin Login - SOKMEAN</title>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #122d88 0%, #000000 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
        }

        .login-box {
            background: white;
            padding: 3rem;
            border-radius: 16px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
            text-align: center;
            width: 350px;
        }

        h1 {
            color: #2563eb;
            margin-bottom: 2rem;
        }

        input {
            width: 100%;
            padding: 14px;
            margin: 10px 0;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-sizing: border-box;
            font-size: 16px;
        }

        button {
            width: 100%;
            padding: 14px;
            background: #2563eb;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            margin-top: 1rem;
        }

        button:hover {
            background: #1d4ed8;
        }

        .error {
            color: #dc2626;
            background: #fef2f2;
            padding: 10px;
            border-radius: 6px;
            margin-bottom: 1rem;
        }

        a {
            color: #64748b;
            text-decoration: none;
            display: block;
            margin-top: 1.5rem;
        }
    </style>
</head>

<body>
    <div class="login-box">
        <h1>🔐 Admin Login</h1>
        <% if (error) { %>
            <div class="error">
                <%= error %>
            </div>
            <% } %>
                <form action="/admin-login" method="POST">
                    <input type="text" name="username" placeholder="Username" required autocomplete="off">
                    <input type="password" name="password" placeholder="Password" required autocomplete="off">
                    <button type="submit">Login</button>
                </form>
                <a href="/">← Back to Store</a>
    </div>
</body>

</html>